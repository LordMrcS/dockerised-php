version: '2'

services:
    web:
        image: nginx:latest
        ports:
            - "8080:80"
        volumes:
            - ./www:/www
            - ./site.conf:/etc/nginx/conf.d/default.conf
        networks:
            - www-network
    php:
        image: php:fpm
        volumes:
            - ./www:/www
        networks:
            - www-network

networks:
    www-network:
        driver: bridge
